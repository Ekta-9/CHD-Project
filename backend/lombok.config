# Stops Lombok from searching parent directories for more config files
config.stopBubbling = true
# Marks Lombok-generated methods with @lombok.Generated so JaCoCo excludes
# them from coverage - keeps the coverage number about real hand-written logic
lombok.addLombokGeneratedAnnotation = true
